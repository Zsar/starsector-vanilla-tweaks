# Updating to a new Starsector version
1. update metadata
	- game version in mod_info.json
	- mod version in mod_info.json and zsarVanillaTweaks.version
2. execute extract-sources.bash
3. rebuild jar from generated-sources and src
